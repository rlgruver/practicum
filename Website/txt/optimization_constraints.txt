#Separate entries with a comma and a line break. Only use constants that #are entered in the Constants section.

CONSTRAINTS
y = x-2, 
z = tx         #Shows and example of a constraint using a constant (t).

