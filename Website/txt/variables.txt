#Separate entries with a comma and a line break. ALL variables used in the 
#Objective Function and Constraints sections MUST be listed here.
#List variables in the format "var in [domain lb, domain ub]" where
#lb = lower bound, ub = upper bound.

VARIABLES
X1 in [-100, 100],
X2 in [-100, 100]
